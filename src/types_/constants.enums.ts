/* eslint-disable no-unused-vars */

export enum ThemeKeys {
  LIGHT = "light",
  DARK = "dark",
  SYSTEM = "system",
}

export enum Paths {
  LANDING = "/",
  SIGN_UP = "/sign-up",
  LOGIN_IN = "/log-in",
}