/* eslint-disable no-unused-vars */

export enum ThemeKeys {
  LIGHT = "light",
  DARK = "dark",
  SYSTEM = "system",
}

export enum Paths {
  LANDING = "/",
  REGISTER = "/register",
  LOGIN_IN = "/log-in",
}